[global]
   workgroup = WORKGROUP
   server string = OpenScan3 Projects Share
   map to guest = Bad User
   log file = /var/log/samba/log.%m
   max log size = 1000
   security = user
   load printers = no
   printing = bsd
   disable spoolss = yes
   obey pam restrictions = yes
   unix password sync = no
   pam password change = no

[openscan-projects]
   comment = OpenScan3 Projects (read/write)
   path = /var/openscan3/projects
   browseable = yes
   guest ok = yes
   read only = no
   writeable = yes
   force user = openscan
   force group = openscan
   create mask = 0664
   directory mask = 2775

